/**
 * Admin MCP Tool Definitions
 *
 * Re-exports from @sentinel/shared to maintain backwards compatibility.
 * The actual definitions live in the shared package to avoid circular dependencies.
 */

export * from '@sentinel/shared';
